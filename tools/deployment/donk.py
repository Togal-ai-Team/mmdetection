class TESTER():
    threshold = 0.5
    SLIDED_INFERENCE_THRESHOLD = 40_000_000
    def test(self):
        print(self.SLIDED_INFERENCE_THRESHOLD)
        print(SLIDED_INFERENCE_THRESHOLD)

test=TESTER()
test.test()